Chatty progress → If safe, stop/cancel and relaunch with quiet/warning-only flags or an `awk`/`sed` filter.
Unsafe to restart? Let it finish.
{{#if hub}}Alternatively, set its monitor to `ambient` or `off`.{{/if}}
